# IMPORTANT: This is a template. Never commit actual secrets to version control!
# Use Kubernetes secrets management (e.g., Sealed Secrets, External Secrets Operator, Vault)

# DO NOT deploy this file as-is. You must:
# 1. Remove the placeholder key
# 2. Add your actual secrets using a secrets management solution

apiVersion: v1
kind: Secret
metadata:
  name: favo-chain-secrets
  namespace: favo-chain
  labels:
    app: favo-chain
  annotations:
    # Add this annotation if using external secrets operator
    # external-secrets.io/provider: aws-secretsmanager
type: Opaque
data: {}
  # Example structure - replace with your actual secret management solution
  # These should be managed by a secrets operator like:
  # - HashiCorp Vault
  # - AWS Secrets Manager with External Secrets Operator
  # - Sealed Secrets
  #
  # Validator private keys (base64 encoded):
  # validator-1-key: <BASE64_ENCODED_PRIVATE_KEY>
  # validator-2-key: <BASE64_ENCODED_PRIVATE_KEY>
  # validator-3-key: <BASE64_ENCODED_PRIVATE_KEY>
  # validator-4-key: <BASE64_ENCODED_PRIVATE_KEY>
  #
  # BLS keys (for favobft consensus):
  # validator-1-bls-key: <BASE64_ENCODED_BLS_KEY>
