# IMPORTANT: This is a template. Never commit actual secrets to version control!
# Use Kubernetes secrets management (e.g., Sealed Secrets, External Secrets Operator, Vault)

apiVersion: v1
kind: Secret
metadata:
  name: favo-chain-secrets
  namespace: favo-chain
  labels:
    app: favo-chain
type: Opaque
stringData:
  # Example structure - replace with your actual secret management solution
  # These should be managed by a secrets operator like:
  # - HashiCorp Vault
  # - AWS Secrets Manager with External Secrets Operator
  # - Sealed Secrets
  
  # Validator private keys (base64 encoded in data: section, or plaintext in stringData:)
  # validator-1-key: "YOUR_VALIDATOR_1_PRIVATE_KEY"
  # validator-2-key: "YOUR_VALIDATOR_2_PRIVATE_KEY"
  # validator-3-key: "YOUR_VALIDATOR_3_PRIVATE_KEY"
  # validator-4-key: "YOUR_VALIDATOR_4_PRIVATE_KEY"
  
  # BLS keys (for favobft consensus)
  # validator-1-bls-key: "YOUR_VALIDATOR_1_BLS_KEY"
  
  placeholder: "REPLACE_WITH_ACTUAL_SECRETS"
